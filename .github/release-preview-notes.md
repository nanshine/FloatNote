- Apple Silicon（M1/M2/M3/M4 及后续芯片）请选择 `.dmg` 文件名包含 `aarch64` 的版本。
- Intel Mac 请选择 `.dmg` 文件名包含 `x86_64` 的版本。
- Windows x86_64 用户请选择 `-setup.exe` 安装包。
- 已安装包含更新器的版本，可以在「设置 → 通用 → 应用更新」中升级；`.sig` 为更新验签文件，无需手动打开。
